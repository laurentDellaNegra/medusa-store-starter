---
import Button from "./Button.astro";
---

<section class="hero">
  <div class="hero__left">
    <p class="hero__tag">Handcrafted in Madagascar</p>
    <h1 class="hero__title">The Art of<br /><em>Raphia</em> Weaving</h1>
    <p class="hero__desc">
      Each Rafia bag is a testament to centuries-old Malagasy craftsmanship,
      where every strand tells a story of tradition, elegance, and sustainable
      luxury.
    </p>
    <div class="hero__actions">
      <Button visual="primary" href="/shop">Discover Collection</Button>
      <Button visual="ghost" href="/#story">
        Our Story
        <svg width="20" height="12" viewBox="0 0 20 12" fill="none">
          <path
            d="M14 1L19 6M19 6L14 11M19 6H1"
            stroke="currentColor"
            stroke-width="1"></path>
        </svg>
      </Button>
    </div>
  </div>
  <div class="hero__right">
    <div class="hero__image">
      <div class="hero__bag">
        <svg viewBox="0 0 300 380" fill="none">
          <ellipse
            cx="150"
            cy="90"
            rx="60"
            ry="70"
            fill="none"
            stroke="#4A3228"
            stroke-width="2.5"
            opacity="0.7"></ellipse>
          <path
            d="M70 160 C70 140,80 125,100 120 L200 120 C220 125,230 140,230 160 L240 330 C240 350,225 360,210 360 L90 360 C75 360,60 350,60 330 Z"
            fill="url(#bagGrad)"
            stroke="#4A3228"
            stroke-width="1.5"></path>
          <g opacity="0.25">
            <line x1="80" y1="160" x2="220" y2="160" stroke="#4A3228" stroke-width="0.8"></line>
            <line x1="75" y1="185" x2="225" y2="185" stroke="#4A3228" stroke-width="0.8"></line>
            <line x1="72" y1="210" x2="228" y2="210" stroke="#4A3228" stroke-width="0.8"></line>
            <line x1="68" y1="235" x2="232" y2="235" stroke="#4A3228" stroke-width="0.8"></line>
            <line x1="65" y1="260" x2="235" y2="260" stroke="#4A3228" stroke-width="0.8"></line>
            <line x1="63" y1="285" x2="237" y2="285" stroke="#4A3228" stroke-width="0.8"></line>
            <line x1="110" y1="125" x2="100" y2="355" stroke="#4A3228" stroke-width="0.8"></line>
            <line x1="150" y1="120" x2="150" y2="358" stroke="#4A3228" stroke-width="0.8"></line>
            <line x1="190" y1="125" x2="200" y2="355" stroke="#4A3228" stroke-width="0.8"></line>
          </g>
          <rect x="170" y="280" width="35" height="45" rx="2" fill="#FFFCF7" opacity="0.8"></rect>
          <line x1="178" y1="295" x2="198" y2="295" stroke="#B8956A" stroke-width="0.8"></line>
          <line x1="178" y1="302" x2="193" y2="302" stroke="#B8956A" stroke-width="0.8"></line>
          <defs>
            <linearGradient id="bagGrad" x1="60" y1="120" x2="240" y2="360">
              <stop offset="0%" stop-color="#D4C5B2"></stop>
              <stop offset="50%" stop-color="#C4AD92"></stop>
              <stop offset="100%" stop-color="#BFA88C"></stop>
            </linearGradient>
          </defs>
        </svg>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    display: grid;
    grid-template-columns: 1fr;
    position: relative;
    overflow: hidden;
  }

  @media (min-width: 1024px) {
    .hero {
      height: 100vh;
      min-height: 700px;
      grid-template-columns: 1fr 1fr;
    }
  }

  .hero__left {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 8rem 2rem 3rem;
    position: relative;
    z-index: var(--z-content);
  }

  @media (min-width: 768px) {
    .hero__left {
      padding: 10rem 3rem 4rem;
    }
  }

  @media (min-width: 1024px) {
    .hero__left {
      padding: 0 6rem;
    }
  }

  .hero__tag {
    font-size: var(--fs-caption);
    letter-spacing: var(--ls-ultra);
    text-transform: uppercase;
    color: var(--color-gold-text);
    margin-bottom: 2rem;
    font-weight: var(--fw-regular);
    opacity: 0;
    transform: translateY(30px);
    animation: fadeUp 1s ease 0.8s forwards;
  }

  .hero__title {
    font-family: var(--font-display);
    font-size: var(--fs-heading-hero);
    font-weight: var(--fw-light);
    line-height: var(--lh-tight);
    color: var(--color-espresso);
    margin-bottom: 2rem;
    opacity: 0;
    transform: translateY(40px);
    animation: fadeUp 1s ease 1s forwards;
  }

  .hero__title em {
    font-style: italic;
    color: var(--color-gold-text);
  }

  .hero__desc {
    font-size: var(--fs-body-md);
    line-height: var(--lh-relaxed);
    color: var(--color-espresso-soft);
    max-width: 440px;
    margin-bottom: 3rem;
    opacity: 0;
    transform: translateY(40px);
    animation: fadeUp 1s ease 1.2s forwards;
  }

  .hero__actions {
    display: flex;
    gap: 1.5rem;
    align-items: center;
    opacity: 0;
    transform: translateY(30px);
    animation: fadeUp 1s ease 1.4s forwards;
  }

  .hero__right {
    position: relative;
    overflow: hidden;
    background: var(--color-sand-light);
    height: 60vh;
  }

  @media (min-width: 1024px) {
    .hero__right {
      height: auto;
    }
  }

  .hero__image {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      var(--color-sand) 0%,
      var(--color-gold-light) 50%,
      var(--color-sand-light) 100%
    );
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    animation: fadeIn 1.5s ease 0.6s forwards;
  }

  .hero__bag {
    width: 320px;
    height: 380px;
  }

  .hero__bag :global(svg) {
    width: 100%;
    height: 100%;
    filter: drop-shadow(0 20px 60px rgba(44, 24, 16, 0.2));
  }
</style>
