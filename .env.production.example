# ─── Domain ────────────────────────────────────────────
# Your domain name (e.g., example.com). Use "localhost" for IP-only access.
# This is set automatically by ./deploy.sh interactive setup.
DOMAIN=localhost

# ─── Postgres ─────────────────────────────────────────
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me

# ─── Medusa Backend ───────────────────────────────────
# For production, regenerate with: openssl rand -hex 32
JWT_SECRET=change_me
COOKIE_SECRET=change_me

# CORS origins — set to your domain (comma-separated if multiple)
# For HTTPS domain:
# STORE_CORS=https://example.com
# ADMIN_CORS=https://example.com
# AUTH_CORS=https://example.com
# For IP-only / localhost testing:
STORE_CORS=http://localhost
ADMIN_CORS=http://localhost
AUTH_CORS=http://localhost

# ─── Storefront (build-time) ─────────────────────────
# Medusa publishable API key — set automatically by deploy.sh after seeding.
PUBLIC_MEDUSA_PUBLISHABLE_KEY=placeholder_will_be_replaced_after_seed

# The public URL where the storefront is accessible
PUBLIC_BASE_URL=http://localhost

# Default region code
PUBLIC_DEFAULT_REGION=dk

# Stripe publishable key (optional — leave empty to disable)
PUBLIC_STRIPE_KEY=
